import { ComponenteAplicacion } from './componente-aplicacion.model';

describe('ComponenteAplicacion', () => {
  it('should create an instance', () => {
    expect(new ComponenteAplicacion()).toBeTruthy();
  });
});
