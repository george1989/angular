import { IntegracionAplicacion } from './integracion-aplicacion.model';

describe('IntegracionAplicacion', () => {
  it('should create an instance', () => {
    expect(new IntegracionAplicacion()).toBeTruthy();
  });
});
