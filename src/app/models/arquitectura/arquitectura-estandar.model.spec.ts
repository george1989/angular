import { ArquitecturaEstandar } from './arquitectura-estandar.model';

describe('ArquitecturaEstandar', () => {
  it('should create an instance', () => {
    expect(new ArquitecturaEstandar()).toBeTruthy();
  });
});
