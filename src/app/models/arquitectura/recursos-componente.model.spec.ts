import { RecursosComponente } from './recursos-componente.model';

describe('RecursosComponente', () => {
  it('should create an instance', () => {
    expect(new RecursosComponente()).toBeTruthy();
  });
});
