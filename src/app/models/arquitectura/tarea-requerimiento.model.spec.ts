import { TareaRequerimiento } from './tarea-requerimiento.model';

describe('TareaRequerimiento', () => {
  it('should create an instance', () => {
    expect(new TareaRequerimiento()).toBeTruthy();
  });
});
