import { HitoRequerimiento } from './hito-requerimiento.model';

describe('HitoRequerimiento', () => {
  it('should create an instance', () => {
    expect(new HitoRequerimiento()).toBeTruthy();
  });
});
