import { DocumentacionArquitectura } from './documentacion-arquitectura.model';

describe('DocumentacionArquitectura', () => {
  it('should create an instance', () => {
    expect(new DocumentacionArquitectura()).toBeTruthy();
  });
});
