import { ERespuestaSimple } from './erespuesta-simple.model';

describe('ERespuestaSimple', () => {
  it('should create an instance', () => {
    expect(new ERespuestaSimple()).toBeTruthy();
  });
});
